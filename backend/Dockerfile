# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build gift-redemption-backend`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t gift-redemption-backend`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system gift-redemption-backend && \
          adduser --system -G gift-redemption-backend gift-redemption-backend

COPY dist/gift-redemption-backend gift-redemption-backend
RUN chown -R gift-redemption-backend:gift-redemption-backend .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix gift-redemption-backend --omit=dev -f install

CMD [ "node", "gift-redemption-backend" ]
